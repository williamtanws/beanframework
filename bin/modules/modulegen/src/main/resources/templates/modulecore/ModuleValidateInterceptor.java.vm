package ${modulegroup}.${moduleartifact}core.interceptor;

import com.beanframework.common.context.InterceptorContext;
import com.beanframework.common.exception.InterceptorException;
import com.beanframework.common.interceptor.AbstractValidateInterceptor;
import ${modulegroup}.${moduleartifact}.domain.${Moduleartifact};

public class ${Moduleartifact}ValidateInterceptor extends AbstractValidateInterceptor<${Moduleartifact}> {

	@Override
	public void onValidate(${Moduleartifact} model, InterceptorContext context) throws InterceptorException {
		super.onValidate(model, context);

	}
}
