package ${modulegroup}.${moduleartifact}core.converter.dto;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.beanframework.common.converter.AbstractDtoConverter;
import com.beanframework.common.converter.DtoConverter;
import com.beanframework.common.exception.ConverterException;
import ${modulegroup}.${moduleartifact}.domain.${Moduleartifact};
import ${modulegroup}.${moduleartifact}core.data.${Moduleartifact}Dto;

public class ${Moduleartifact}DtoConverter extends AbstractDtoConverter<${Moduleartifact}, ${Moduleartifact}Dto> implements DtoConverter<${Moduleartifact}, ${Moduleartifact}Dto> {

	protected static Logger LOGGER = LoggerFactory.getLogger(${Moduleartifact}DtoConverter.class);

	@Override
	public ${Moduleartifact}Dto convert(${Moduleartifact} source) throws ConverterException {
		return super.convert(source, new ${Moduleartifact}Dto());
	}
}
